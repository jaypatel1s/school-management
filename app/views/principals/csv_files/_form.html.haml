= form_with model: [:school, csv_file], local: true, html: { multipart: true }, url: url, class: 'form' do |f|
  .row
    .col-lg-6.col-sm-6
      .form-group.mb-4
        %label.label.text-secondary.required= t('issues.csv_files.table.name')
        = f.text_field :name, class: "form-control h-55 #{error?(@csv_file.errors, :name)}", value: f.object.name, placeholder: 'Name', required: true
        = print_error(@csv_file.errors, :name)
    .col-lg-6.col-sm-6
      .form-group.mb-4
        %label.label.text-secondary.required= t('issues.csv_files.table.email')
        = f.email_field :email, class: "form-control h-55 #{error?(@csv_file.errors, :email)}", value:  f.object.email, placeholder: 'Email'
        = print_error(@csv_file.errors, :email)
    .col-lg-12.col-sm-6
      .form-group.mb-4
        %label.label.text-secondary.required= t('issues.csv_files.table.role')
        = f.select :role, User.roles.keys.map { |role| [t("roles.#{role}"), role] }, {}, class: "form-control h-55 #{error?(@csv_file.errors, :role)}"
        = print_error(@csv_file.errors, :role)
    .col-lg-6.col-sm-6
      .form-group.mb-4
        %label.label.text-secondary.required= t('issues.csv_files.table.password')
        = f.password_field :password, class: "form-control h-55 #{error?(@csv_file.errors, :password)}", value: f.object.password, placeholder: 'Password', required: true
        = print_error(@csv_file.errors, :password)
    .col-lg-12
      .d-flex.flex-wrap.gap-3
        %button.btn.btn-primary.py-2.px-4.fw-medium.fs-16.text-white{ type: 'submit', data: { confirm: 'Do you want to save the changes?' } }
          %i.ri-add-line.text-white.fw-medium
          = t('issues.csv_files.table.save')
